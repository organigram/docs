import DeploymentAddresses, {
  DeploymentNetworkHeading,
  DeploymentNetworksTableOfContents,
  getDeploymentNetworks,
  getNetworkAnchor,
  getNetworkLabel
} from '../../components/DeploymentAddresses'

export const metadata = { title: '🕸️ Networks', order: 5.5 }

export const networks = getDeploymentNetworks()

# Supported Networks 🕸️

Organigram.ai supports the networks below for the web platform and the open-source libraries. Published protocol contract addresses are tracked in [packages/protocol/deployments.json](https://github.com/organigram/organigram/blob/main/packages/protocol/deployments.json).

<DeploymentNetworksTableOfContents networks={networks} />

{networks.map(network => (
  <section key={network.chainId}>
    <DeploymentNetworkHeading id={getNetworkAnchor(network).slice(1)}>
      {getNetworkLabel(network)}
    </DeploymentNetworkHeading>
    <DeploymentAddresses chainId={network.chainId} />
  </section>
))}

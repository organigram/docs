import Button from '@mui/material/Button'
import Grid from '@mui/material/Grid'

import Link from '../../components/Link'

export const metadata = { title: '🗄️ Certified storage', order: 4.5 }

# Certified storage 🗄️

Organigram uses IPFS to preserve governance metadata, procedure records, certified proofs, and supporting files on decentralized storage infrastructure.

This helps organizations keep records accessible and verifiable without depending only on Organigram servers.

## What is IPFS?

IPFS is a decentralized file storage system. Instead of identifying files by where they are hosted, IPFS identifies them by their content.

When a file is stored on IPFS, it receives a content identifier. If the content changes, the identifier changes too. This makes IPFS useful for governance evidence because records can be referenced, checked, and preserved over time.

## What Organigram stores

Depending on the workflow, Organigram can store:

- organization metadata
- organ and procedure metadata
- proposal records
- supporting files
- certified proof documents
- encrypted files shared with selected recipients

## Workspace-based access groups

To allow private file sharing and encrypted storage, Organigram uses workspace-based
access groups. Each workspace has its own set of encryption keys and access control
rules, so members can share files securely within the workspace without exposing
them publicly on IPFS.

When a file is uploaded to IPFS with workspace encryption enabled, it is encrypted
in the browser before being sent to IPFS. The file can only be decrypted by users
who are currently members of the workspace, and access is revoked when they leave.

This means that the files uploaded are not only time-stamped and publicly verifiable, but also end-to-end encrypted.

You can learn more about our encryption model in the [encryption](/docs/advanced/encryption) chapter.

## Why decentralized storage matters

Decentralized storage helps reduce vendor lock-in. Governance records are designed to remain readable and verifiable even if Organigram stopped operating as a company.

It also gives organizations a stronger continuity story: decisions, proofs, and metadata can be preserved outside the application database.

## Pricing

Included storage is available for normal usage. Additional IPFS storage above 1GB is charged according to the public prices listed on the [Pricing](/pricing) page.

<Grid container my={3} justifyContent='center'>
  <Grid item>
    <Link href={'/pricing'}>
      <Button variant='contained'>View storage pricing</Button>
    </Link>
  </Grid>
</Grid>

## Related pages

- [Sponsored transactions](/docs/certified/sponsored-transactions)
- [Advanced signatures](/docs/certified/advanced-signatures)
- [Pricing](/pricing)
